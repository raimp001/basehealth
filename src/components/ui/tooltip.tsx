import React from 'react';

export const TooltipProvider: React.FC = ({ children }) => (
  <div>{children}</div>
);

export const Tooltip: React.FC = ({ children }) => (
  <div>{children}</div>
);

export const TooltipTrigger: React.FC = ({ children }) => (
  <div>{children}</div>
);

export const TooltipContent: React.FC = ({ children }) => (
  <div>{children}</div>
);

