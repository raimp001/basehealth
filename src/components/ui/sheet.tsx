import React from 'react';

export const Sheet: React.FC = ({ children }) => (
  <div>{children}</div>
);

export const SheetTrigger: React.FC = ({ children }) => (
  <div>{children}</div>
);

export const SheetContent: React.FC = ({ children }) => (
  <div>{children}</div>
);

